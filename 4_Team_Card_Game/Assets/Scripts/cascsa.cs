using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cascsa : MonoBehaviour
{
    int script = 0;
    // Start is called before the first frame update
    void Start()
    {
        script = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
